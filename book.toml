[book]
title = "weir"
description = "A durable, high-throughput write buffer for Rust."
authors = ["weir contributors"]
src = "docs"
language = "en"

[output.html]
# The two themes visitors actually land on. theme/weir.css remaps both onto the
# weir palette from demo/weir.css; the other three (rust, coal, ayu) keep their
# stock mdBook colours and only pick up the structural styling.
default-theme = "light"
preferred-dark-theme = "navy"
git-repository-url = "https://github.com/miki-przygoda/weir"
git-repository-icon = "fab-github"
edit-url-template = "https://github.com/miki-przygoda/weir/edit/main/docs/{path}"
mathjax-support = false
no-section-label = false
# theme/weir.css is loaded after mdBook's own stylesheets and overrides them by
# cascade order. It is a presentation layer only — no markdown is aware of it.
additional-css = ["theme/weir.css"]

[output.html.fold]
enable = true
level = 1

[output.html.print]
enable = false

[output.html.search]
enable = true
limit-results = 30
teaser-word-count = 30
use-boolean-and = true
boost-title = 2
boost-hierarchy = 1
boost-paragraph = 1
expand = true
heading-split-level = 3

[rust]
edition = "2021"

# Link checking. Validates internal markdown cross-references on every
# `mdbook build`. CI installs the mdbook-linkcheck binary; local builds
# without it still succeed (`optional = true`).
#
# follow-web-links = false: external URLs (http/https) are not fetched.
# Internal `.md` links and anchor fragments are checked.
[output.linkcheck]
optional = true
follow-web-links = false
warning-policy = "error"
exclude = []
