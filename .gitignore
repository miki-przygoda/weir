# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# RustRover
.idea/

# mdBook build output (CI builds and publishes; never committed)
book/

# Other
.DS_Store
PLAN.md
todos
__pycache__/

# Local working docs (plans, scratch notes — not part of the repo)
.workdocs/
# Subagent-driven-development scratch (ledgers, briefs, review packages).
.superpowers/

# Schedules generated per-launch by chaos/run-docker-soak.sh — the base
# schedule is the source of truth; these are derived and disposable.
chaos/schedules/generated/

# Local CI runner: `act` is the supported way to run CI locally (images pinned
# in .actrc; see deploy/ci-local/README.md). Anything else in that directory is
# a personal script and stays untracked — opinionated tooling nobody owns
# becomes another thing claiming to check what it no longer checks.
deploy/ci-local/*
!deploy/ci-local/README.md
scripts/ci-local
target-docker/
# The load job appends BENCH lines here; harmless in CI's fresh checkout,
# but it accumulates in a working tree when the suite is run locally.
load_results.jsonl
